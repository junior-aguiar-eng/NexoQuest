name = "nexoquiz-mcp"
main = "dist/server/worker.js"
compatibility_date = "2024-12-01"
# account_id — set via `wrangler login` or the CLOUDFLARE_ACCOUNT_ID env var

routes = [
  { pattern = "mcp.quizhp.com/*", zone_name = "quizhp.com" }
]
